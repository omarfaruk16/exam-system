# Overrides for docker-compose (optional). Compose has sane defaults baked in.
POSTGRES_USER=exam
POSTGRES_PASSWORD=exam_dev_password
POSTGRES_DB=exam
POSTGRES_PORT=5436
REDIS_PORT=6379
PGBOUNCER_PORT=6432
