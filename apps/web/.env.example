# The SPA calls the API at a relative /api path (proxied in dev, nginx in prod), so no API URL
# is needed here. Override the dev proxy target only if your API runs elsewhere:
# VITE_API_PROXY_TARGET=http://localhost:4100

VITE_INSTITUTION_NAME=University of Rajshahi
